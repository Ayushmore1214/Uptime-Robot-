apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
data:
  # Values must be base64 encoded.
  # Run these commands and paste the output:
  # echo -n 'admin' | base64
  # echo -n 'YourSuperStrongPassword123' | base64
  # echo -n 'your-rds-endpoint-from-terraform-output' | base64
  # echo -n 'kstaxdb' | base64
  DB_USER: YWRtaW4=
  DB_PASSWORD: # PASTE_YOUR_BASE64_ENCODED_PASSWORD_HERE
  DB_HOST: # PASTE_YOUR_BASE64_ENCODED_RDS_ENDPOINT_HERE
  DB_NAME: a3N0YXhkYg==
